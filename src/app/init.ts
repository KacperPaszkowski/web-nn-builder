import { v4 as uuidv4 } from 'uuid'
import { convTransform, inputTransform, maxPool2dTransform } from './transforms'
import { Conv2D, Input, MaxPool2D } from './nodedef'

export const initialNodes = [
    // Input(),
    // Conv2D(),
    // MaxPool2D()
]